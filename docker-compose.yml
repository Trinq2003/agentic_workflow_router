version: '3.8'

services:
  # API Service
  api:
    profiles: ["prod"]
    image: ${API_IMAGE:-agentic-router:latest}
    container_name: agentic-router-api
    environment:
      - PYTHONPATH=/app/src
      - LOG_LEVEL=INFO
    networks:
      - agentic-router-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    ports:
      - "${API_PORT:-8000}:8000"

  # API Service for development (hot reload, bind mounts)
  api-dev:
    profiles: ["dev"]
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    container_name: agentic-router-api-dev
    command: uvicorn src.api.app:app --host 0.0.0.0 --port 8000 --reload
    volumes:
      - .:/app
      - /app/__pycache__
    environment:
      - PYTHONPATH=/app/src
      - LOG_LEVEL=DEBUG
      - ENV=development
    ports:
      - "${API_PORT:-8000}:8000"
      
  redis:
    image: redis:7-alpine
    container_name: agentic-router-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_PORT:-6379}:6379"
    networks:
      - agentic-router-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

networks:
  agentic-router-network:
    driver: bridge
    name: agentic-router-network

volumes:
  redis_data:
    name: agentic-router-redis-data
